{{!Renders a metric group and all its children, one table each}}
<div class="panel panel-default">
<div class="panel-heading">
<a id="{{name}}"><h3 class="panel-title">{{name}}</h3></a>
</div>
<div class="panel-body">

<table class='table table-bordered table-hover'>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th>Description</th>
  </tr>
    {{#metrics}}
  <tr>
    <td><tt>{{name}}</tt></td>
    {{! Is this a stats metric? }}
    {{?mean}}
    <td>
      Last (of {{count}}): <strong>{{last}}</strong>.
      Min: {{min}}, max: {{max}}, avg: {{mean}}</td>
    {{/mean}}
    {{^mean}}
    <td>
      {{human_readable}}
    </td>
    {{/mean}}
    <td>
      {{description}}
    </td>
  </tr>
  {{/metrics}}
</table>

{{! Recurse into all child groups }}
{{#child_groups}}
{{>www/metric_group.tmpl}}
{{/child_groups}}
</div>
</div>
